- name: Définir le mot de passe root MySQL
  become: yes
  shell: |
    mysql -u root -e --password='root' "ALTER USER 'root'@'localhost' IDENTIFIED WITH caching_sha2_password BY '{{ root_password }}';"
    mysql -u root -e --password='root' "FLUSH PRIVILEGES;"
  changed_when: false
